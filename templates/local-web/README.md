# __PROJECT_NAME__

This application was generated by ThimbleDB __THIMBLE_VERSION__.

```powershell
npm run dev
```

Open `http://127.0.0.1:5173`.
Open `http://127.0.0.1:5173/studio/` for the packaged Studio.

The local authority:

- binds only to `127.0.0.1`
- uses the local storage provider
- enables the development identity only outside production
- stores encrypted user-scoped data
- configures a snapshot collection with title and modification-time indexes
- proxies `/api` and `/studio` so the app and Studio share an origin

Run the project check:

```powershell
npm run doctor
npm run build
```

Before deploying, replace the development identity with Microsoft Entra or
another OIDC provider and review the main ThimbleDB security and deployment
documentation.
